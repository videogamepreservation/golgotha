

;; Translation instructions :
;; Translate File/Edit/View/Window/Map/Terrain, etc.
;; Translate the first string (everything in quotes), do not translate the second string
;; pair

;;            Translate                 Don't Translate
;;            this colum                this colum
;----------------------------------------------------------------
(add_sub_menu "Options"
              ("Add Thread"            "add_thread")
              ("Exit"                  "exit"))
;              ("Save Visited Sites"    "save_visited")
;              ("Load Visited Sites"    "load_visited")
;              _



;(add_sub_menu "Search"
;              ("Add site to visit"     "add_site")
;              ("Options"                "Options")
;              -
;              )